## 需求
飞书多维表格仪表盘上的一个气泡象限图的插件，让用户自选选择入参数据，自动生成相应的气泡图。

### 0.5 上架相关的优化
- [ ] 多语言
- [ ] 系统稳定性优化
- [ ] 主题色兼容

### 0.4
- [x] 在数据源下面增加一个数据范围选项配置，用于选择数据源下具体视图的数据范围，点击下拉菜单可选择：
    - 全部数据，默认选中，位于第一个，且固定不变。选中时数据范围为当前数据源的所有数据
    - 其他选项依次为当前数据源的各个视图，按顺序依次展示。选中时数据范围为该视图所展示的数据

### 0.3
- [x] 横纵轴和气泡大小可以使用公式字段作为数据来源
- [x] 优化气泡大小的显示逻辑，现在如果气泡大小的数值过大的话，可能会造成超出屏幕的情况，但是只限制最大最小值的话，当多个数据的大小都超出极值的话就无法体现大小差异了。→可以做一个按比例换算到极值大小内的算法。
- [x] 进行颜色优化，可以彩色气泡，也可以单色气泡
- [x] 在气泡旁边用引线显示气泡名称
- [x] 如果气泡大小字段没有配置的话，tooltip 中也不显示大小的信息了，因为都是用的默认大小
- 必填

### 0.2
- [x] 横纵轴字段除了可选择数值类型，也可以选择单选类型，两个轴可以一个是数值类型，一个是单选类型，也可以都是单选或数值
- [x] 单选类型的轴在图表上用类目轴的形式展示，按单选字段的选项顺序依次展示轴刻度和刻度标签

### 0.1
- [x] 完成基础的气泡图插件，可以正常在飞书多维表格的仪表盘上配置和保存
- [x] 用户输入5个入参数据，都是下拉输入框，分别是「数据源」，对应气泡图的「气泡名称」、「横轴」、「纵轴」、「气泡大小」
    - [x] 数据源：从当前多维表格中选择一个工作表作为数据源，必选
    - [x] 气泡名称：从数据源中选择一个字段作为气泡的名称，非必选，如果选了则只显示有气泡名称的数据，如果为空，则显示所有数据
    - [x] 横轴：从数据源中选择一个字段作为气泡图的横轴，必选
    - [x] 纵轴：从数据源中选择一个字段作为气泡图的纵轴，必选
    - [x] 气泡大小：从数据源中选择一个字段作为气泡的大小，非必选，如果选了则根据该字段的值来设置气泡的大小，如果为空，则所有气泡的大小相同
- [x] 气泡图的显示规则
    - [x] 轴标题显示为所选字段的字段名
    - [x] 气泡名称相同的气泡，会重复显示
