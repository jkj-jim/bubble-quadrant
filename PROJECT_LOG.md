<!-- 项目日志
日志的记录上，尽量保持简洁；
简要罗列已完成的功能和待完成的功能；
记录关键的实现的功能/结果/问题，有必要的话尽可能简练的概述实现方案，避免记录无意义的中间状态；
 -->
# 飞书气泡图插件 - 项目日志

## 项目概述
为飞书多维表格开发一个仪表盘插件，实现可配置的气泡图，支持用户选择数据源、横轴、纵轴、气泡大小和气泡名称字段。

## 当前状态（2025-11-24）

### 🎉 已完成
- [x] 基础项目架构搭建（React + TypeScript + Vite）
- [x] 集成飞书官方 UI 组件（@douyinfe/semi-ui）
- [x] 集成 ECharts 实现气泡图渲染
- [x] 完成数据源和字段选择器 UI
- [x] 实现数据获取逻辑（`getData`/`getPreviewData`）
- [x] 配置保存和加载功能（`saveConfig`/`getConfig`）
- [x] 视图模式切换（`config`/`view`）及实时预览
- [x] 优化用户体验（移除欢迎页面，与官方插件行为一致）
- [x] **支持类目轴**：单选字段可作为 X/Y 轴的类目轴
- [x] **支持混合轴模式**：
  - 双数值轴（传统气泡图）
  - X轴类目 + Y轴数值（混合轴）
  - X轴数值 + Y轴类目（混合轴）
  - 双类目轴（散点图）
- [x] **完成高级刷新机制重构**：彻底解决配置保存在部分场景下不刷新、闪烁、白屏等一系列问题。
- [x] **支持公式字段**：
  - 识别数值类型的公式字段作为数值轴/气泡大小
  - 识别文本类型的公式字段作为类目轴
- [x] **气泡大小优化**：
  - 实现线性缩放算法，将数值映射到固定像素范围 (10px-80px)，防止气泡过大溢出
  - 未配置大小字段时显示固定大小 (10px)
- [x] **百分比显示优化**：
  - 智能识别百分比格式的数值/公式字段
  - 在轴标签和 Tooltip 中正确显示百分比（如 `9%` 而非 `0.09`）
- [x] **UI/UX 细节打磨**：
  - 调整坐标轴标题位置，避免遮挡
  - 优化字段选择器交互（添加"清除"链接，支持 Tooltip 提示）
  - 优化 Tooltip 显示逻辑（未配置大小时隐藏大小信息）
  - 优化数据处理逻辑（未配置名称时显示所有散点，配置名称时过滤空名称并取消聚合）
- [x] **主题适配（暗黑模式）**：
  - 监听飞书宿主环境主题变化
  - Semi UI 组件自动跟随主题切换
  - ECharts 图表颜色动态适配主题（使用 CSS 变量 + useMemo）
  - 处理 Semi UI 特殊 RGB 格式变量
- [x] **国际化 (i18n)**：
  - 使用飞书 SDK 获取准确的语言设置
  - 支持中文、英文、日文三语切换
  - LoadApp 模式避免初始化白屏
- [x] **图表渲染体验优化**：
  - 消除加载时白屏闪烁
  - 无缝数据刷新（保留旧图表直到新数据到来）
  - Hover 时强制显示当前气泡标签

### 🔍 关键问题解决

#### 1. 数据同步问题（2025-11-13）

重构数据流，采用 `onDataChange` → `getConfig()` 权威模式：事件处理上移至 App.tsx，将事件作为变更通知而非数据源，重新获取权威配置并更新 state，通过 React 标准数据流驱动更新。为后续的彻底修复奠定了基础。

#### 2. 用户体验优化（2025-11-15）

在 `useDashboard` 中添加 `isConfig`（`state === 'config' || state === 'create'`），删除欢迎页面逻辑。点击「添加插件」直接进入配置界面，从 view → config 正确加载已保存配置，与官方插件行为保持一致。

#### 3. 类目轴支持（2025-11-18）

扩展 `useFields` 识别单选字段，新建 `useFieldOptions` 获取选项，扩展 `BubbleChartConfig` 支持 `xFieldType`/`yFieldType`，重构 `useData3` 支持 4 种轴类型组合，扩展 `BubbleChart` 组件动态生成 ECharts 配置。类目显示顺序与多维表格设置一致。

#### 4. 高级刷新机制重构（2025-11-18）

采用 `useEffect(..., [])` 在组件挂载时注册全局、持久的 `onConfigChange` 和 `onDataChange` 监听器。对事件处理增加"防抖 (`setTimeout`) + 深度比对 (`JSON.stringify`)"机制，合并密集事件、忽略内容重复的更新。重构 `useData` hook 使其成为数据和坐标轴选项的唯一权威来源，彻底分离数据逻辑和UI状态。

#### 5. 公式字段与数据展示优化（2025-11-22）

扩展字段类型检测逻辑，解析公式字段的 `formatter` 属性以区分数值/文本/百分比。在数据处理层统一解析百分比字符串，在展示层统一格式化为百分比。

#### 6. 数据范围配置（2025-11-22）

新增 `useViews` hook 获取数据源下的所有视图，在配置面板增加"数据范围"下拉框，更新 `useData3` 在获取数据时传入 `viewId` 参数。支持按视图过滤数据，默认选中"全部数据"。

#### 7. 多彩气泡模式（2025-11-23）

在配置面板增加"开启多彩模式"开关，引入飞书风格色板，开启后循环分配颜色。提升了密集数据下的可视性。

#### 8. 数据标签功能（2025-11-23）

使用 ECharts 的 `label` 属性在气泡上方显示名称，启用 `labelLayout.hideOverlap` 自动隐藏重叠标签。

#### 9. 样式配置重构（2025-11-23）

提取 `CHART_STYLES` 样式常量对象集中管理配置，创建 `createAxisConfig()` 工厂函数统一生成轴配置。代码量减少约 80 行，消除 X/Y 轴重复配置。

#### 10. 配置自动填充优化（2025-11-23）

新建图表时自动选择第一个数据源和字段；切换数据源时自动重置并重新填充字段；处理字段数量不足时的边缘情况。

#### 11. 交互增强：Hover 高亮效果（2025-11-23）

使用 ECharts v5 的 `blur` 功能，配置 `emphasis.focus: 'self'`。Hover 时当前气泡高亮，其他气泡淡化并隐藏标签。

#### 12. UI 细节优化（2025-11-23）

移除表单标签后的冒号，"开启多彩模式"改为复选框样式，"名称常显"复选框仅在选择气泡名称字段时显示。

#### 13. 主题适配与暗黑模式（2025-11-24）

Semi UI：监听 `bitable.bridge.onThemeChange`，设置 `body` 的 `theme-mode` 属性。ECharts：使用 `useMemo` + CSS 变量动态解析颜色（替代 `MutationObserver`）。处理 Semi UI 的 `--semi-grey-*` 系列 RGB 格式变量（`230,232,234` → `rgb(230,232,234)`）。

#### 14. 国际化 (i18n)（2025-11-24）

使用 `bitable.bridge.getLanguage()` 获取语言，实现 `LoadApp` 模式避免初始化白屏，支持中英日三语。

#### 15. 图表渲染体验优化（2025-11-24）

移除 Loading 遮罩层改为透明背景，修改清空逻辑为 `!loading && data.length === 0` 保留旧图表，`emphasis.label.show: true` 使 Hover 时显示标签，在图表更新 `useEffect` 依赖中添加 `chartStyles` 确保主题变化时触发更新。


## 技术栈
- 框架：React 18 + TypeScript
- 构建：Vite
- SDK：@lark-base-open/js-sdk（飞书开放能力）
- UI：@douyinfe/semi-ui（飞书官方组件）
- 图表：ECharts

## 核心文件
- `src/App.tsx` - 主应用组件
- `src/components/BubbleChart.tsx` - 气泡图组件
- `src/hooks/useData3.ts` - 数据获取和处理逻辑
- `src/hooks/useDashboard.ts` - 仪表盘状态管理
- `src/hooks/useTables.ts` - 数据表管理
- `src/hooks/useFields.ts` - 字段管理

