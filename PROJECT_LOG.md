<!-- 项目日志
日志的记录上，尽量保持简洁；
简要罗列已完成的功能和待完成的功能；
记录关键的变更和的问题，描述上尽可能简练的概述关键情况，避免记录无意义的中间状态；
 -->
# 飞书气泡图插件 - 项目日志

## 项目概述
为飞书多维表格开发一个仪表盘插件，实现可配置的气泡图，支持用户选择数据源、横轴、纵轴、气泡大小和气泡名称字段。

## 当前状态（2025-11-18）

### 🎉 已完成
- [x] 基础项目架构搭建（React + TypeScript + Vite）
- [x] 集成飞书官方 UI 组件（@douyinfe/semi-ui）
- [x] 集成 ECharts 实现气泡图渲染
- [x] 完成数据源和字段选择器 UI
- [x] 实现数据获取逻辑（`getData`/`getPreviewData`）
- [x] 配置保存和加载功能（`saveConfig`/`getConfig`）
- [x] 视图模式切换（`config`/`view`）及实时预览
- [x] 优化用户体验（移除欢迎页面，与官方插件行为一致）
- [x] **支持类目轴**：单选字段可作为 X/Y 轴的类目轴
- [x] **支持混合轴模式**：
  - 双数值轴（传统气泡图）
  - X轴类目 + Y轴数值（混合轴）
  - X轴数值 + Y轴类目（混合轴）
  - 双类目轴（散点图）
- [x] **完成高级刷新机制重构**：彻底解决配置保存在部分场景下不刷新、闪烁、白屏等一系列问题。
- [x] **支持公式字段**：
  - 识别数值类型的公式字段作为数值轴/气泡大小
  - 识别文本类型的公式字段作为类目轴
- [x] **气泡大小优化**：
  - 实现线性缩放算法，将数值映射到固定像素范围 (10px-80px)，防止气泡过大溢出
  - 未配置大小字段时显示固定大小 (10px)
- [x] **百分比显示优化**：
  - 智能识别百分比格式的数值/公式字段
  - 在轴标签和 Tooltip 中正确显示百分比（如 `9%` 而非 `0.09`）
- [x] **UI/UX 细节打磨**：
  - 调整坐标轴标题位置，避免遮挡
  - 优化字段选择器交互（添加"清除"链接，支持 Tooltip 提示）
  - 优化 Tooltip 显示逻辑（未配置大小时隐藏大小信息）
  - 优化数据处理逻辑（未配置名称时显示所有散点，配置名称时过滤空名称并取消聚合）

### 🔍 关键问题解决

#### 1. 数据同步问题（2025-11-13）

**问题**：config 模式保存配置后，view 模式显示旧数据，需手动刷新。

**根源**：`saveConfig()` 触发 `onDataChange` 事件，竞态条件导致旧配置覆盖新配置。

**方案**：重构数据流，采用 `onDataChange` → `getConfig()` 权威模式
- 事件处理上移至 App.tsx
- 将事件作为变更通知，而非数据源
- 重新获取权威配置并更新 state
- 通过 React 标准数据流驱动更新

**结果**：为后续的彻底修复奠定了基础，但未能完全解决问题。

#### 2. 用户体验优化（2025-11-15）

**问题**：添加插件时显示欢迎页面，需点击「配置图表」才能进入配置界面，与官方行为不一致。

**方案**：
- 在 `useDashboard` 中添加 `isConfig`（`state === 'config' || state === 'create'`）
- 删除欢迎页面逻辑，统一使用 `isConfig` 判断配置界面
- 配置加载使用 `state !== 'create'`（而非 `!isConfig`）确保从 view 切换时正确加载配置

**结果**：
- ✅ 点击「添加插件」直接进入配置界面
- ✅ 从 view → config 正确加载已保存配置
- ✅ 与官方插件行为保持一致

#### 3. 类目轴支持（2025-11-18）

**问题**：用户需要使用单选字段作为坐标轴类目。

**方案**：
- 扩展 `useFields` 识别单选字段作为类目字段
- 新建 `useFieldOptions` hook 获取单选字段的所有选项
- 扩展 `BubbleChartConfig` 类型，支持 `xFieldType`/`yFieldType`
- 重构 `useData3` 支持 4 种轴类型组合
- 扩展 `BubbleChart` 组件动态生成 ECharts 配置

**结果**：
- ✅ 支持单选字段作为 X/Y 轴类目
- ✅ 类目显示顺序与多维表格设置一致

#### 4. 高级刷新机制重构（2025-11-18）

**问题**：在混合轴等复杂场景下，保存配置后视图不刷新，或出现闪烁、白屏等一系列问题。

**根源**：核心是 `saveConfig` 后的 `onConfigChange` 事件与 React 组件的 `state` 更新之间存在竞态条件。不当的 `useEffect` 依赖项和混乱的数据流设计加剧了问题。

**最终方案**：
- **全局监听**：采用 `useEffect(..., [])` 在组件挂载时注册全局、持久的 `onConfigChange` 和 `onDataChange` 监听器，确保不错过任何 SDK 事件。
- **防抖与深度比对**：对事件处理增加“防抖 (`setTimeout`) + 深度比对 (`JSON.stringify`)”机制，合并密集事件、忽略内容重复的更新，从根源解决“闪烁”问题。
- **重构数据流**：重构 `useData` hook，使其成为数据和坐标轴选项的唯一权威来源，彻底分离数据逻辑和UI状态，解决了连锁的 `ReferenceError` (白屏)问题。

**结果**：
- ✅ 彻底解决了所有已知场景下的刷新、闪烁和白屏问题。
- ✅ 应用架构更清晰，数据流更健壮。
- ✅ 相关经验已沉淀至 `FEISHU_DASHBOARD_TIPS.md`。

#### 5. 公式字段与数据展示优化（2025-11-22）

**问题**：
1. 公式字段无法作为轴或大小来源。
2. 百分比数值显示为小数（0.09）而非百分比（9%）。

**方案**：
- **公式支持**：扩展字段类型检测逻辑，解析公式字段的 `formatter` 属性以区分数值/文本/百分比。
- **百分比优化**：在数据处理层统一解析百分比字符串，在展示层统一格式化为百分比。

#### 6. 数据范围配置（2025-11-22）

**问题**：用户希望能够选择特定视图的数据进行展示，而不仅仅是全量数据。

**方案**：
- 新增 `useViews` hook 获取数据源下的所有视图。
- 在配置面板增加“数据范围”下拉框，支持选择“全部数据”或具体视图。
- 更新 `useData3` hook，在获取数据时传入 `viewId` 参数进行过滤。

**结果**：
- ✅ 支持按视图过滤数据。
- ✅ 默认选中“全部数据”，兼容原有逻辑。

#### 7. 多彩气泡模式（2025-11-23）

**问题**：当气泡重叠时，单色气泡难以区分。

**方案**：
- 在配置面板增加“开启多彩模式”开关。
- 引入飞书风格色板，开启后循环分配颜色。

**结果**：
- ✅ 支持一键切换单色/多彩模式。
- ✅ 提升了密集数据下的可视性。
- ✅ 修复了开关状态无法保持的 Bug。
- ✅ 修复了查看模式下多彩设置未生效的问题。

## 技术栈
- 框架：React 18 + TypeScript
- 构建：Vite
- SDK：@lark-base-open/js-sdk（飞书开放能力）
- UI：@douyinfe/semi-ui（飞书官方组件）
- 图表：ECharts

## 核心文件
- `src/App.tsx` - 主应用组件
- `src/components/BubbleChart.tsx` - 气泡图组件
- `src/hooks/useData3.ts` - 数据获取和处理逻辑
- `src/hooks/useDashboard.ts` - 仪表盘状态管理
- `src/hooks/useTables.ts` - 数据表管理
- `src/hooks/useFields.ts` - 字段管理

