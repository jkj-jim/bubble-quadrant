# 飞书气泡图插件 - 项目日志

## 项目概述
为飞书多维表格开发一个仪表盘插件，实现可配置的气泡图，支持用户选择数据源、横轴、纵轴、气泡大小和气泡名称字段。

## 当前状态（2025-11-18）

### 🎉 已完成
- [x] 基础项目架构搭建（React + TypeScript + Vite）
- [x] 集成飞书官方 UI 组件（@douyinfe/semi-ui）
- [x] 集成 ECharts 实现气泡图渲染
- [x] 完成数据源和字段选择器 UI
- [x] 实现数据获取逻辑（`getData`/`getPreviewData`）
- [x] 配置保存和加载功能（`saveConfig`/`getConfig`）
- [x] 视图模式切换（`config`/`view`）及实时预览
- [x] 修复数据同步问题（config → view 模式切换后自动刷新）
- [x] 优化用户体验（移除欢迎页面，与官方插件行为一致）
- [x] **支持类目轴（2025-11-18）**：单选字段可作为 X/Y 轴的类目轴，按用户在多维表格中设定的选项顺序显示
- [x] **支持混合轴模式（2025-11-18）**：
  - 双数值轴（传统气泡图）
  - X轴类目 + Y轴数值（混合轴）
  - X轴数值 + Y轴类目（混合轴）
  - 双类目轴（散点图）

### 🐛 已知问题

#### 问题：混合轴配置之间切换时 view 状态不刷新

**现象描述**：
- 图表配置为混合轴状态（如：X轴=单选字段（类目），Y轴=数值字段）时
- 打开 config 弹窗，修改其中一个字段为另一个单选或数值字段（保持混合轴状态）
- 点击「保存并查看」
- **view 状态的图表显示旧配置数据，需要手动刷新卡片才会更新**

**正常场景**（不会出现问题）：
- ✅ 双数值 → 混合轴：刷新正常
- ✅ 混合轴 → 双数值：刷新正常

**问题场景**：
- ❌ 混合轴 → 混合轴（如：单选A+数值 → 单选B+数值）：大概率不刷新

**问题类型**：偶发 / 可复现

**日志分析结果**：

从 TEST_LOG.md 的日志可见：

❌ **场景A（混合轴→混合轴）- 不刷新**（如：修改单选字段A为单选字段B，保持另一轴不变）：
```
[App] 开始保存配置
[App] 配置保存成功
[App] 更新本地config状态并触发refresh
[App] 保存完成
...（缺少 [App] onDataChange 事件触发 日志）
```

✅ **场景B（双数值→混合轴）- 正常刷新**（如：数值+数值 → 单选+数值）：
```
[App] 开始保存配置
[App] 配置保存成功
[App] 更新本地config状态并触发refresh
[App] 保存完成
[App] onDataChange 事件触发  ← 关键区别：onDataChange 触发了
...
```

**根本原因**：
- 飞书 SDK 的 `dashboard.onDataChange` 事件在特定场景下不可靠
- 特别是在「混合轴配置之间切换」时，该事件有约 80% 概率不触发
- 虽然已在 `handleSave` 中调用 `setConfig` 更新本地状态，但 view 模式的 `useEffect` 中没有获取到最新配置

**已尝试的修复方案**：
1. ✅ 在 `handleSave` 中保存后立即调用 `setConfig` 同步本地状态
2. ⚠️ 添加第二个 `useEffect` 监听 state 变化，尝试强制刷新（初步测试效果待验证）
3. ❌ 直接使用 `refreshKey` 机制，会导致额外的不必要刷新

**建议的长期解决方案**：
- 方案A：切换到「后端计算模式」（需要在 `saveConfig` 时提供 `dataConditions` 和 `series` 配置）
  - 优点：由飞书后端负责数据计算，前端无需关心数据获取逻辑
  - 缺点：灵活性降低，只能使用飞书提供的聚合能力
- 方案B：在 `App.tsx` 中维护一个 `savedConfig` ref，用于保存服务器端权威配置，与本地 `config` state 分离
  - 优点：完全控制数据流，不依赖不可靠的 `onDataChange` 事件
  - 缺点：代码复杂度增加，需要管理两份配置状态
- 方案C：在切换到 view 状态时，增加 timeout 延迟，确保数据同步完成
  - 优点：简单快速
  - 缺点：治标不治本，用户体验差

**当前状态**：该问题仍在排查和修复中，已通过添加详细日志定位问题。

### 🔍 关键问题解决

#### 1. 数据同步问题（2025-11-13）

**问题**：config 模式保存配置后，view 模式显示旧数据，需手动刷新。

**根源**：`saveConfig()` 触发 `onDataChange` 事件，竞态条件导致旧配置覆盖新配置。

**方案**：重构数据流，采用 `onDataChange` → `getConfig()` 权威模式
- 事件处理上移至 App.tsx
- 将事件作为变更通知，而非数据源
- 重新获取权威配置并更新 state
- 通过 React 标准数据流驱动更新

**结果**：彻底消除竞态条件，确保数据可预测。详见 `FEISHU_DASHBOARD_TIPS.md`。

#### 2. 用户体验优化（2025-11-15）

**问题**：添加插件时显示欢迎页面，需点击「配置图表」才能进入配置界面，与官方行为不一致。

**方案**：
- 在 `useDashboard` 中添加 `isConfig`（`state === 'config' || state === 'create'`）
- 删除欢迎页面逻辑，统一使用 `isConfig` 判断配置界面
- 配置加载使用 `state !== 'create'`（而非 `!isConfig`）确保从 view 切换时正确加载配置

**结果**：
- ✅ 点击「添加插件」直接进入配置界面
- ✅ 从 view → config 正确加载已保存配置
- ✅ 与官方插件行为保持一致

**关键细节**：配置加载必须用 `state !== 'create'` 而不是 `!isConfig`，否则会跳过 `config` 状态的加载（已于 FEISHU_DASHBOARD_TIPS.md 中记录）

#### 3. 类目轴支持（2025-11-18）

**问题**：用户需要使用单选字段作为坐标轴类目。

**实现方案**：
- 扩展 `useFields` 识别单选字段作为类目字段
- 新建 `useFieldOptions` hook 获取单选字段的所有选项（按用户设定的顺序）
- 扩展 `BubbleChartConfig` 类型，支持 `xFieldType`/`yFieldType` 区分数值/类目
- 重构 `useData3`，支持 4 种轴类型组合：
  - 双数值（传统气泡图）
  - X类目+Y数值（混合轴）
  - X数值+Y类目（混合轴）
  - 双类目（散点图）
- 扩展 `BubbleChart` 组件，根据轴类型动态生成 ECharts 配置
- 在 `App.tsx` 中根据字段类型显示（数值/类目）标签

**技术挑战**：
- 数据传递链路复杂：fieldOptions 需要在 config 状态通过 hook 实时获取，在 view 状态使用已保存的权威数据
- 使用 React hooks 依赖数组确保数据变化触发重新计算

**结果**：
- ✅ 支持单选字段作为 X/Y 轴类目
- ✅ 类目显示顺序与多维表格设置一致
- ✅ 向后兼容，默认数值轴
- ✅ 支持多语言（中文界面和提示）

**经验教训**：数据流设计时，需区分「实时配置状态」和「权威保存状态」，并明确数据获取时机和位置。详见 FEISHU_DASHBOARD_TIPS.md。


## 技术栈
- 框架：React 18 + TypeScript
- 构建：Vite
- SDK：@lark-base-open/js-sdk（飞书开放能力）
- UI：@douyinfe/semi-ui（飞书官方组件）
- 图表：ECharts

## 核心文件
- `src/App.tsx` - 主应用组件
- `src/components/BubbleChart.tsx` - 气泡图组件
- `src/hooks/useData3.ts` - 数据获取和处理逻辑
- `src/hooks/useDashboard.ts` - 仪表盘状态管理
- `src/hooks/useTables.ts` - 数据表管理
- `src/hooks/useFields.ts` - 字段管理

